# LuminaX Target Design Contract

## Contract Status

本文件是后续阶段统一工作台的目标设计合同，不描述 Phase 0 当前已实现的 `LuminaXApp` 界面。

Phase 0 当前界面仍使用既有的聊天、仪表盘和报告视图切换。下文的双核心布局、角色模板解析和移动端分段交互是后续实施必须遵守的批准合同，不能据此声称它们已经上线。

## Product Character

LuminaX 是明亮、克制、可持续使用的经营运营中心。界面应让门店运营人员在同一工作台中查看事实、理解原因、采取行动并确认权限边界。它是工作工具，不是营销页面或深色数据大屏：信息应密集但有秩序，重点由数据优先级和操作语境决定，而不是由装饰决定。

## Color Roles

- 工作表面使用白色与浅灰色：页面背景保持明亮，主工作面为白色，分区和表格背景使用低对比浅灰。
- 主文字使用近黑色，次级文字和边框使用中性灰，确保长时间阅读与数据扫描的清晰度。
- LuminaX 黄 `#FFE600` 是不可替换的品牌色，用于品牌识别、主要操作、当前焦点和重要确认，不作为大面积底色或错误提示色。
- 红、绿、蓝仅表达业务状态：红色表示风险、失败或异常，绿色表示达成或健康，蓝色表示信息、链接或中性进行状态。不要用这些颜色替代权限、优先级或装饰。
- 所有图表必须使用语义一致的状态映射，并在颜色之外提供标签、数值、图例或图形差异。

## Typography

- 使用系统无衬线字体和中文系统字体回退，正文以清晰、稳定的尺寸呈现，不随视口宽度缩放。
- 工作台标题应紧凑有力，面板标题用于扫描，正文、表格和对话保持可读的正常字距；不得使用负字距。
- KPI 数值可使用较高字重与较大字号，但不能压过筛选、时间范围、单位和变化说明。
- 表格、标签、图表注释和权限说明必须保留足够对比度，不能用过小或过淡的文字隐藏重要信息。

## Density and Spacing

- 采用 4px 递进间距系统，常用内部间距为 8px、12px、16px 和 24px；边界、对齐和行高应服务于快速扫描。
- 页面分区是全宽带状或无框布局，卡片只用于单个 KPI、重复条目、弹窗和真正需要边界的工具，不得将卡片嵌套在卡片中。
- 控件、表格列、工具栏图标和图表容器要使用稳定的尺寸与响应式约束，动态数据不能引起布局跳动。
- 圆角保持小且一致，避免胶囊化的业务界面；信息密度优先于空旷的展示感。

## Unified Workbench

统一工作台将经营数据核心与分析决策核心并置，而不是把聊天体验拆成独立页面。顶部提供全局范围与时间筛选；筛选结果应同时驱动 KPI、图表、数据表、对话上下文和可执行建议。

桌面端采用双核心布局：左侧或主区域承载可扫描的经营事实、趋势、明细和筛选，右侧或辅助区域承载 Agent 对话、解释、追问和建议。两侧都是一等工作区，宽度应在内容和任务需要下弹性调整，不能固化为营销式的主从比例。操作人员应能在不离开当前范围的情况下从数据到解释再回到数据。

## Role Templates and Permission States

目标工作台只内置区域经理模板和通用默认模板。`templateId` 仅有两个固定值：`regional_manager` 和 `default`。

- 区域经理模板 `regional_manager` 默认突出辖区概况、异常门店、门店下钻、经营归因和周报入口。
- 通用默认模板 `default` 提供标准经营概览、指标分析、AI 对话和报表入口。
- `manager` 固定解析为 `regional_manager`。
- `super_admin` 固定解析为 `default`，并显示管理后台入口。
- 其他现有角色固定解析为 `default`；未知角色或解析失败也安全回退到 `default`。
- 模板映射由系统确定，管理员不能选择、创建或重新分配模板。
- 模板只决定组件顺序、默认图表、首页重点、推荐问题和空状态文案；模板不能授予或扩大数据与操作权限。
- 权限状态必须在界面中明确表达：已授权可直接操作，受限操作展示范围或原因，不具备权限的操作不可误导为可用，已禁用用户不能继续访问。
- 数据范围、模块访问、操作权限和指标访问是独立但可共同呈现的维度。管理员可在允许的边界内细化这些权限，但不能更改模板映射。
- 管理操作和受限结果需要保留清晰的原因、影响范围和下一步责任人，不能只给出模糊的“无权限”。

## Admin Shell

管理端是与业务工作台一致的受控操作壳，而不是另一套视觉语言。它应提供清晰的导航、当前管理员身份、返回业务工作台的路径，以及用户、权限和指标登记的可扫描工作区。

权限管理不提供角色模板选择。管理员应检查用户角色、状态、数据范围、模块与操作权限；模板由固定角色映射自动解析。指标管理应明确区分草稿、已发布、已停用和校验失败，并在危险操作前展示受影响范围与确认步骤。

## Responsive Behavior

- 桌面端保留双核心工作台：经营数据与 Agent 决策区同时可见，筛选和关键上下文保持一致。
- 平板端可收紧辅助区域，但不能丢失当前筛选、权限状态或可返回的工作上下文。
- 从 `360px` 宽度起，移动端使用分段式工作台，在“经营数据”和“分析决策”之间切换；分段控件以两个等宽且可触达的选项呈现，切换不重置筛选、对话、权限提示或未完成操作。
- 从 `360px` 宽度起，工作台采用单列且内容受视口宽度约束：页面不得产生横向溢出，分段控件、筛选器、KPI、图表、表单和操作不得相互遮挡。表格可在其自身容器内横向查看次要列，但不得造成页面级横向滚动；关键状态与操作不可仅依赖悬停。

## Loading, Empty, Error, and Denied States

- 加载状态使用骨架或局部进度，保持面板尺寸稳定并说明正在加载的区域。
- 空状态说明当前筛选范围内没有什么数据，并给出合适的筛选调整或数据准备路径；不得用虚构数据填充。
- 错误状态清楚指出失败的工作区、可重试操作和可供排障的非敏感信息，不能泄露密钥、令牌或数据库凭据。
- 拒绝状态明确说明该动作或数据范围受限，并指向拥有处理权限的角色或管理员路径。

## Interaction and Motion

- 筛选、图表和对话之间的联动应即时可感知，并保留当前范围与来源信息。
- 动效用于确认状态变化、面板展开和数据刷新，时长宜短且可预测；不得以连续动画、闪烁或夸张跳动干扰运营扫描。
- 图标按钮使用熟悉的图标并提供悬停或键盘可达的提示；文字按钮只用于明确命令。
- 危险操作、权限改变和指标发布必须要求明确确认，且确认文案反映实际影响。

## Accessibility

- 文本、图标、焦点状态和业务状态满足可读的对比度要求；`#FFE600` 上的文字必须选择足够深的前景色。
- 所有核心流程可使用键盘完成，焦点可见且顺序符合工作流。
- 表单有持久标签和具体错误信息；图表提供可访问的标题、摘要或表格替代信息。
- 不以颜色、位置、悬停或动画作为传达权限、风险、加载或成功的唯一方式。

## Prohibited Patterns

- 禁止深色唯一主题、深黑大面积工作面和“深色数据大屏”式布局；LuminaX 的默认方向是明亮运营中心。
- 禁止蓝紫渐变、装饰性渐变、发光圆球、散景背景和无业务意义的装饰图形。
- 禁止固定的“左侧数据、右侧 32% 对话”布局；桌面端必须是可适应任务的双核心工作台，移动端必须是分段式工作台。
- 禁止用大幅营销文案、巨大英雄区、伪造数据、3D 图表或卡片套卡片来替代可操作的信息层级。
- 禁止把颜色当作唯一状态标识，或在权限不足、数据为空和请求失败时留下误导性的可点击操作。
